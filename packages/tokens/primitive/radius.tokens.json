{
  "$schema": "https://tr.designtokens.org/format/",
  "radius": {
    "$description": "Single --radius base variable. Tailwind generates full scale via multipliers. Role-based, not computed from nesting.",
    "base": { "$type": "dimension", "$value": "0.5rem", "$description": "8px — change this one value to rescale all radii" }
  }
}
