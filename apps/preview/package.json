{
  "name": "@lab-ui/preview",
  "version": "0.0.0",
  "private": true,
  "description": "Lab UI token preview and theme matrix — Vite app that exercises the Tailwind v4 preset and raw CSS vars against the four output sectors.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173 --strictPort",
    "test:e2e": "playwright test",
    "test:e2e:install": "playwright install --with-deps chromium"
  },
  "dependencies": {
    "@lab-ui/tokens": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@tailwindcss/vite": "^4.0.0",
    "@types/node": "^22.0.0",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.7.0",
    "vite": "^5.4.0"
  }
}
